{% extends 'base.html.twig' %}

{% block body %}

    <h1>Edit Ardoise</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

{% endblock %}

{% block title %}
    {{ parent() }} | Modifier l'ardoise
{% endblock %}

