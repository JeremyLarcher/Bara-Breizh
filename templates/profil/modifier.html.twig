{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Modifier mon profil
{% endblock %}

{% block body %}
    <div class="row m-5 justify-content-center form">
        <div class="col-md-5 border border-3 rounded form-container">
            {{ form_start(ModifierProfil) }}
            {{ form_widget (ModifierProfil) }}
            <div class="m-5">
            <button type ="submit" class="button btn btn-lg btn-primary"> Validé </button>
            </div>
            {{ form_end(ModifierProfil) }}
        </div>
    </div>

{% endblock %}

